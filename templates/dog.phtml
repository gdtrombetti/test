<?php
?>

<div class="container">
    <div class="row">
        <img class="height" src="<? echo $content->getLogo()?>" alt="<? echo $content->getType()?>">
        <p class="margins"><? echo $content->getType()?> - The Dog is mans best friend.  Enough said.</p>
    </div>
</div>